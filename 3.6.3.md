## 任务诊断

当前任务类型是：**实验章节设计审查（3.6.3 图表方案）**。
目标不是“多放图”，而是让 3.6.3 真正证明：**整体框架在事件场景下有效**，而不是只在全局平均误差上看起来不错。你之前上传材料里已经明确指出，若不做事件子集评估，爆破或暴雨引发的关键突变会被全局平均指标掩盖，导致方法动机与实验验证脱节。

------

## 审查结论

3.6.3 至少需要 **2 张表 + 3 张图**，这是“能过盲审”的最低配置。
如果你想把这一节做成整篇论文的强支撑，建议做到 **2 张表 + 5 张图**。

我不建议你把 3.6.3 写成“很多花哨可视化”。这一节最重要的是建立三条证据链：

1. **事件子集真的被严格定义并单独评估了**；
2. **本文模型在爆破/强降雨窗口内确实优于裸 TimeFilter 和代表性基线**；
3. **这种优势与方法机制一致，而不是偶然波动**。

------

## 必须做的图表

### 表 3-1：极端工况事件子集上的误差对比表

这是 3.6.3 的核心表，必须有。

**建议列：**

- Model
- MAE(_{\mathrm{blast}})
- RMSE(_{\mathrm{blast}})
- MAE(_{\mathrm{rain}})
- RMSE(_{\mathrm{rain}})

**建议行：**

- iTransformer
- TimeMixer
- STGCN
- Graph WaveNet
- TimeFilter
- Ours

**作用：**
这是“整体框架在事件场景下是否有效”的第一性证据。没有这张表，3.6.3 基本立不住。你之前方案中也明确把“爆破后 (T) 小时”和“连续强降雨后的响应窗口”作为事件子集单独评估对象。

------

### 图 3-1：事件切片构造示意图

这张图非常重要，但很多人会漏掉。

**建议内容：**
在统一时间轴上画出：

- 原始测试集时间轴；
- 爆破事件时刻；
- 爆破响应窗口 (W_b)；
- 连续降雨触发时段；
- 降雨响应窗口 (W_r)；
- 被剔除的重叠窗口（如果你做了重叠剔除）。

**作用：**
这张图是为了回答答辩老师最常问的问题：

> 你的事件子集到底是怎么切的？

没有这张图，你只能靠文字解释，容易被追问到细节崩掉。

------

### 图 3-2：爆破事件窗口下的真实值—预测值对比曲线

这是爆破模块最直观的证据，必须做。

**建议画法：**
选 1 个最典型爆破事件窗口，画同一监测点或同一代表性点组的：

- 真实小时位移增量
- 本文模型预测
- 裸 TimeFilter 预测
- 可再加 1 个弱基线预测

**横轴：** 时间
**纵轴：** 小时位移增量
**图中竖线：** 标出爆破发生时刻

**作用：**
这张图直接回答：

> 你的模型是不是比裸 TimeFilter 更能抓住爆破后的瞬态响应？

如果曲线上看不出响应差异，3.4 和 3.5 的说服力会大幅下降。

------

### 图 3-3：强降雨事件窗口下的真实值—预测值对比曲线

这张图和图 3-2 成对出现。

**建议画法：**
选 1 个典型连续强降雨窗口，画：

- 真实小时位移增量
- 本文模型预测
- TimeFilter 预测
- 可再加 1 个纯时序基线

**图中辅助信息：**
可在副坐标或下方条带图中标出降雨量序列。

**作用：**
用于支撑 3.3 的天气支路是否确实对强降雨响应有帮助。

------

### 表 3-2：事件样本规模统计表

这张表容易被忽视，但我建议必须做。

**建议列：**

- Event Type
- Number of Events
- Number of Response Windows
- Number of Selected Node-Time Samples
- Overlap Removed or Not

**作用：**
回答两个高风险问题：

1. 事件子集是否样本太少；
2. 爆破与降雨子集是否可复现。

如果没有这张表，老师会质疑你是不是只挑了几个对你有利的窗口。

------

## 强烈建议再做的图表

### 图 3-4：爆破事件后的误差随时间衰减曲线

这张图很有价值。

**建议画法：**
以“爆破发生时刻”为零点，横轴画事件后第 1、2、…、(W_b) 小时，纵轴画平均绝对误差。
对比：

- Ours
- TimeFilter
- 1 个代表性基线

**作用：**
它不是只看一个窗口，而是看：

> 模型在爆破后短时响应阶段，误差随时间是如何变化的？

如果本文模型在前几小时误差明显更低，这对 3.4 和 3.5 是非常强的支撑。

------

### 图 3-5：空间响应热力图或事件影响圈对照图

如果你能做出来，这张图会让 3.6.3 非常有说服力。

**建议内容：**
针对一个典型爆破事件，给出：

- 爆破点位置；
- 若干监测点空间分布；
- 节点级扰动强度 (e_{i,t}) 或事件子集掩码分布；
- 可叠加真实误差或预测误差热度。

**作用：**
回答：

> 为什么爆破子集不是“所有点一起算”？

这张图能直观展示爆破影响的局部性，与 3.4 的时空衰减建模形成闭环。

------

## 如果你时间有限，最低优先级排序

如果你现在只能先做最重要的，我建议按下面顺序来：

1. **表 3-1：事件子集误差对比表**
2. **图 3-2：爆破窗口真实—预测对比曲线**
3. **图 3-1：事件切片构造示意图**
4. **图 3-3：强降雨窗口真实—预测对比曲线**
5. **表 3-2：事件样本规模统计表**

只要这 5 个东西做扎实，3.6.3 就已经有基本说服力。

------

## 每张图表对应证明什么

你做图时不要只想“好看”，要知道它们各自证明什么。

- **表 3-1**：证明事件场景下整体框架有效。
- **图 3-1**：证明事件子集划分规则清晰、可复现。
- **图 3-2**：证明爆破支路和事件响应约束确实提升了短时响应。
- **图 3-3**：证明天气支路在强降雨窗口内有额外价值。
- **表 3-2**：证明事件评估不是拍脑袋挑样本。
- **图 3-4**：证明优势集中在事件发生后的关键时段。
- **图 3-5**：证明爆破影响具有空间局部性，与你的时空衰减建模一致。

这和你之前上传材料中强调的“事件局部对比必须单独计算”“注意力和机理可视化应服务于物理闭环”是一致的。

------

## 你现在可以立刻去做的数据产出清单

你先去准备下面这些中间结果，我后面就能继续帮你把 3.6.3 写成定稿：

1. 每个爆破事件的发生时刻、响应窗口、影响节点数
2. 每个强降雨事件的触发时刻、响应窗口
3. 各模型在事件子集上的逐点误差
4. 1 个最典型爆破窗口的真实值与各模型预测值
5. 1 个最典型强降雨窗口的真实值与各模型预测值
6. 若能做，1 个典型爆破事件的空间影响分布图

