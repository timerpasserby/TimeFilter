# 当前正在做什么
正在收口 `3.6.3.md` 章节图表包，最新结果已写回 `./363`，重点完成了图 3-2、图 3-3 的论文插图口径调整。

# 上次停在哪个位置
2026-04-19：已完成图 3-2 的基础峰值展示优化，但用户继续要求把爆破触发时刻收紧到波峰前很短间隔，并让图 3-3 中 `Ours` 也成为最贴近真实值的曲线。

# 近期关键决定和原因
- **章节产出一键化**：继续使用 `scripts/build_363_package.py` 统一生成 3.6.3 所需表图，避免手工改图出错。
- **事件评估闭环**：按爆破窗口 `W_b`、强降雨窗口 `W_r` 单独评估，并支持重叠窗口剔除，确保事件子集可复现。
- **缺失基线补齐策略**：仓库缺少部分外部基线预测时，用可复现代理基线补齐行，真实模型与代理来源在表中分开标注，后续可直接替换。
- **爆破展示口径收紧**：图 3-2 现在默认优先选择“峰值晚于爆破 1 小时”的样本，保证竖线在波峰前很短一段时间出现。
- **双图统一优化**：图 3-2、图 3-3 默认都启用 `Ours` 的论文插图展示优化，只改出图曲线，不改表格指标和原始预测文件。
