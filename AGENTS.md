> 说明：这个文件只保留最容易遗漏、但必须遵守的项目协作规则。每次开始新会话时，先读 `CONTEXT.md`，再按本文件执行；更长的背景说明放到 `README.md` 和 `ARCHITECTURE.md`。

# 规则优先级

- 当前这次任务里，用户明确提出的要求优先。
- 没有冲突时，按 `AGENTS.md` 执行。
- 项目现状和最近决定以 `CONTEXT.md` 为准。
- 背景说明和长期设计参考 `README.md`、`ARCHITECTURE.md`。

# 回复与协作方式

- 全程用中文回复。
- 用简洁、非技术的语言说明完成了什么、结果是什么，让非技术背景的人也能看懂。
- 明确的子任务直接完成，不需要每一步确认。
- 只有遇到真正影响结果的分叉点，再停下来确认。
- 任务完成后不要主动提出优化建议或替代方案；如果有必须提醒的问题，最多一句话说明。

# 每次开始前

1. 先读 `CONTEXT.md`，了解当前进度、上次停留位置和最近决定。
2. 再判断是否需要读 `README.md` 和 `ARCHITECTURE.md`：
   - 简单任务：读完 `CONTEXT.md` 直接开始。
   - 复杂任务、新功能、运行方式变化：再读完整文档。
3. 检查项目根目录是否有 `AGENTS.md`，有则优先按本文件执行。

# Python 运行规则

- 运行项目里的 Python 脚本时，不要直接用 `python`。
  - 用 `/opt/homebrew/Caskroom/miniforge/base/envs/tslib/bin/python` 
- 安装、运行、测试命令都尽量走项目本地虚拟环境。

# 执行中的硬规则

- 需求不明确时，先问清楚再动手，不要自行猜测后大量编写。
- 避免重复运行任务。除非用户明确要求，不要做多轮实验、多次基准测试或自动调参。
- 所有配置项、路径、密钥等统一放配置文件管理，不要硬编码。

# 代码与注释规则

- 每个文件顶部写一段简短注释，说明这个文件是做什么的、和哪些模块有关联。
- 每个函数写一行注释说明用途。
- 复杂逻辑在关键步骤处补中文注释，说明这样做的意图。
- 错误处理要完整，异常必须有明确提示或日志，不能静默失败。
- 代码注释统一使用中文。
- plt可视化 用plt.rcParams['font.sans-serif'] = ['Arial Unicode MS'] 

# 阶段完成后的更新

- 每次阶段性完成后，必须更新 `CONTEXT.md`，内容保持极简，只写：
  - 当前正在做什么
  - 上次停在哪个位置
  - 近期关键决定和原因
- 同步维护 `figures_report.md`：记录用 Python 生成的图表、处理过程和结果文件。
- 同步维护 `method_report.md`：记录使用的方法、算法或模型、数据集、评估指标、调参方式、结果可视化方式和结果评估方式。
- 只有在下面这些情况出现时，才更新 `README.md` 和 `ARCHITECTURE.md`：
  - 新增或删除了一个功能模块
  - 部署方式、运行命令或依赖发生了变化
  - 新增了重要设计决定

# 文档最少应包含的内容

- `README.md`：项目功能简介、技术架构、本地运行方法、部署方法和命令、测试方法和常用命令、搜索记录、已完成功能列表、待办事项。
- `ARCHITECTURE.md`：每个文件或模块的职责、模块之间的调用关系、关键设计决定和原因。
